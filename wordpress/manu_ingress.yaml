apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: manu-ingress
  namespace: manu
spec:
  rules:
  - host: lepreteplume.net
    http:
      paths:
      - backend:
          serviceName: manu-wordpress
          servicePort: 80
